// Глобальные настройки для тестов
// Этот файл выполняется перед каждым тестом

// Увеличиваем таймаут для асинхронных операций
jest.setTimeout(10000);

// Можно добавить глобальные моки или утилиты
// Например, мок для console.log в тестах
// global.console = {
//   ...console,
//   log: jest.fn(),
//   debug: jest.fn(),
//   info: jest.fn(),
//   warn: jest.fn(),
//   error: jest.fn(),
// };
