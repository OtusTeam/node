# ⚔️ Type vs Interface - Быстрое сравнение

## 📊 Таблица различий

| Функция | Interface | Type | Примечание |
|---------|-----------|------|------------|
| **Базовые объекты** | ✅ | ✅ | Оба работают одинаково |
| **Расширение** | `extends` | `&` | Разный синтаксис |
| **Declaration Merging** | ✅ | ❌ | Только Interface |
| **Union типы** | ❌ | ✅ | `"a" \| "b"` |
| **Tuple** | ❌ | ✅ | `[number, string]` |
| **Примитивы** | ❌ | ✅ | `type ID = string` |
| **Mapped types** | ❌ | ✅ | `{ [K in keyof T]: ... }` |
| **Conditional** | ❌ | ✅ | `T extends U ? X : Y` |
| **Классы** | ✅ | ✅ | Оба через `implements` |
| **Производительность** | ✅ | ✅ | Одинаковая |

---

## 🎯 Когда использовать?

### ✅ Используйте **INTERFACE** для:

```typescript
// 1. Публичного API
interface IPlugin {
    name: string;
    init(): void;
}

// 2. Контрактов классов
interface IRepository<T> {
    save(item: T): void;
}

// 3. Объектов данных
interface IUser {
    id: number;
    name: string;
}

// 4. Когда нужно расширение
interface Window {
    myProp: string;
}
```

### ✅ Используйте **TYPE** для:

```typescript
// 1. Union типов
type Status = "idle" | "loading" | "success";

// 2. Tuple
type Point = [number, number];

// 3. Примитивов
type ID = string;
type Age = number;

// 4. Intersection
type A = { x: number };
type B = { y: number };
type C = A & B;

// 5. Mapped types
type Readonly<T> = {
    readonly [K in keyof T]: T[K];
};

// 6. Conditional
type NonNullable<T> = T extends null ? never : T;
```

---

## 🔥 Наглядные примеры

### 1. Declaration Merging

```typescript
// ✅ INTERFACE - объединяются
interface User {
    name: string;
}

interface User {
    age: number;
}

// Результат: { name: string; age: number; }

// ❌ TYPE - ошибка!
type User = { name: string };
type User = { age: number };  // ❌ Duplicate identifier
```

---

### 2. Union Types

```typescript
// ✅ TYPE - поддерживает union
type Status = "pending" | "approved" | "rejected";
type ID = string | number;

// ❌ INTERFACE - не поддерживает
interface Status = "pending" | "approved";  // ❌ Синтаксическая ошибка
```

---

### 3. Расширение

```typescript
// INTERFACE - extends
interface Animal {
    name: string;
}

interface Dog extends Animal {
    breed: string;
}

// TYPE - intersection (&)
type Animal = {
    name: string;
};

type Dog = Animal & {
    breed: string;
};
```

---

### 4. Tuple и Примитивы

```typescript
// ✅ TYPE
type Coordinates = [number, number];
type RGB = [number, number, number];
type UserID = string;

// ❌ INTERFACE - не подходит
// interface Coordinates = [number, number];  // Ошибка!
```

---

### 5. Mapped Types

```typescript
// ✅ TYPE - поддерживает
type Partial<T> = {
    [K in keyof T]?: T[K];
};

type Readonly<T> = {
    readonly [K in keyof T]: T[K];
};

// ❌ INTERFACE - не поддерживает
```

---

## 🎨 Визуальное сравнение

```
┌─────────────────────────────────────────────────────────────┐
│                    INTERFACE                                 │
├─────────────────────────────────────────────────────────────┤
│  ✅ Объекты                                                  │
│  ✅ Расширение (extends)                                     │
│  ✅ Declaration Merging                                      │
│  ✅ Контракты для классов                                    │
│  ❌ Union                                                    │
│  ❌ Tuple                                                    │
│  ❌ Примитивы                                                │
│  ❌ Mapped types                                             │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                        TYPE                                  │
├─────────────────────────────────────────────────────────────┤
│  ✅ Объекты                                                  │
│  ✅ Intersection (&)                                         │
│  ✅ Union (|)                                                │
│  ✅ Tuple                                                    │
│  ✅ Примитивы                                                │
│  ✅ Mapped types                                             │
│  ✅ Conditional types                                        │
│  ❌ Declaration Merging                                      │
└─────────────────────────────────────────────────────────────┘
```

---

## 💡 Простое правило

```
┌──────────────────────────────────────────────────┐
│                                                  │
│   📦 Объекты и контракты → INTERFACE            │
│                                                  │
│   🔧 Всё остальное → TYPE                       │
│                                                  │
└──────────────────────────────────────────────────┘
```

---

## 🤔 Сложные случаи

### React Component Props

```typescript
// Оба варианта OK, но Interface популярнее
interface Props {
    name: string;
    age: number;
}

// ИЛИ

type Props = {
    name: string;
    age: number;
};
```

### API Response

```typescript
// Type лучше для union
type ApiResponse<T> = 
    | { success: true; data: T }
    | { success: false; error: string };
```

### Библиотеки

```typescript
// Interface для возможности расширения пользователями
export interface PluginOptions {
    theme: string;
    lang: string;
}

// Пользователи могут расширить:
declare module 'your-lib' {
    interface PluginOptions {
        customField: boolean;
    }
}
```

---

## 📈 Производительность

- **Компиляция**: Одинаковая
- **Runtime**: Нет разницы (стираются при компиляции)
- **IDE**: Одинаковая поддержка

---

## ✨ Best Practices

1. **Консистентность** - выберите стиль и придерживайтесь его
2. **Public API** - используйте Interface
3. **Internal types** - можно использовать Type
4. **React** - любой (но часто Interface)
5. **Utility types** - только Type

---

## 🚀 Запуск примеров

```bash
npm run start_type_vs_interface
```

---

**Итог:** Оба хороши! Главное - использовать правильный инструмент для правильной задачи. 🎯

