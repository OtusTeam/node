extends layout

block content
  h1= title
  if user
    p Welcome to #{title}, #{user}
  else
    p Not Welcome to #{title}
    
  p Views #{views}
  if user
    a(href="/logout") Logout
  else
    a(href="/login") Login
