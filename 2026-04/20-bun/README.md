# 20-bun

Три минимальных примера под Bun:

- `bun/` — чистый `Bun.serve`
- `hono/` — Hono на Bun
- `elysia/` — Elysia на Bun

## Как запустить

В любом из проектов:

```bash
bun install
bun run dev
```

Порт по умолчанию: `3000` (можно переопределить через `PORT`).

