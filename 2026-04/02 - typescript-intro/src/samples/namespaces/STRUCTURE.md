# Структура примеров Namespaces TypeScript

## 📁 Файловая структура

```
src/samples/namespaces/
│
├── index.ts                               # Точка входа, запуск всех примеров
├── README.md                              # Полное руководство
├── NAMESPACES-CHEATSHEET.md               # Быстрая справка
└── STRUCTURE.md                           # Этот файл
│
├── 01-basic-namespace/                    # Базовый Namespace
│   └── geometry.ts                        # Функции геометрии, константы
│
├── 02-nested-namespaces/                  # Вложенные Namespaces
│   └── shapes.ts                          # 2D и 3D фигуры
│
├── 03-namespace-merging/                  # Слияние Namespaces
│   └── validation.ts                      # Валидация (email, phone, password)
│
├── 04-namespace-aliases/                  # Псевдонимы Namespaces
│   └── utils.ts                           # Длинные пути и псевдонимы
│
├── 05-namespace-with-interfaces/          # Namespaces с типами
│   └── models.ts                          # Интерфейсы, типы, енумы, классы
│
└── 06-namespace-vs-module/                # Namespace vs Module
    └── comparison.ts                      # Сравнение двух подходов
```

---

## 🎯 Описание каждого раздела

### 1. Basic Namespace (`01-basic-namespace/`)
**Что изучается:**
- Объявление namespace
- Export функций и констант
- Приватные члены (без export)
- Базовое использование

**Пример:**
```typescript
namespace Geometry {
  export function calculateCircleArea(radius: number) {}
  function privateHelper() {} // Недоступна снаружи
  export const PI = Math.PI;
}
```

**Файлы:**
- `geometry.ts` - функции расчёта площадей (круг, прямоугольник, треугольник)

---

### 2. Nested Namespaces (`02-nested-namespaces/`)
**Что изучается:**
- Вложенные namespaces
- Логическая группировка
- Иерархическая структура
- Интерфейсы внутри namespaces

**Пример:**
```typescript
namespace Shapes {
  export namespace TwoDimensional {
    export class Circle {}
  }
  export namespace ThreeDimensional {
    export class Sphere {}
  }
}
```

**Файлы:**
- `shapes.ts` - 2D (Circle, Rectangle) и 3D (Sphere, Cube) фигуры

---

### 3. Namespace Merging (`03-namespace-merging/`)
**Что изучается:**
- Автоматическое слияние объявлений
- Множественные определения
- Расширение API
- Добавление интерфейсов

**Пример:**
```typescript
namespace Validation {
  export function validateEmail() {}
}
namespace Validation { // Сольётся с первым!
  export function validatePassword() {}
}
```

**Файлы:**
- `validation.ts` - валидация email, phone, password, URL с результатами

---

### 4. Namespace Aliases (`04-namespace-aliases/`)
**Что изучается:**
- Создание псевдонимов (import =)
- Упрощение длинных путей
- Локальные имена
- Улучшение читаемости

**Пример:**
```typescript
namespace Company.Product.Features.Advanced {
  export class Processor {}
}

import Adv = Company.Product.Features.Advanced;
new Adv.Processor(); // Короче!
```

**Файлы:**
- `utils.ts` - глубоко вложенные namespaces и псевдонимы

---

### 5. Namespace with Interfaces (`05-namespace-with-interfaces/`)
**Что изучается:**
- Интерфейсы в namespace
- Типы и type aliases
- Енумы (enums)
- Generic классы
- Repository pattern

**Пример:**
```typescript
namespace Database {
  export interface IUser {}
  export type UserId = number;
  export enum Status {}
  export class Repository<T> {}
}
```

**Файлы:**
- `models.ts` - User, Product интерфейсы, Repository, QueryResult

---

### 6. Namespace vs Module (`06-namespace-vs-module/`)
**Что изучается:**
- Сравнение namespace и module
- Преимущества и недостатки
- Когда использовать каждый подход
- Миграция с namespace на module

**Пример:**
```typescript
// NAMESPACE
namespace MathUtils {
  export function add() {}
}

// MODULE
export function add() {}
```

**Файлы:**
- `comparison.ts` - примеры обоих подходов и детальное сравнение

---

## 🚀 Как запустить

### Все примеры сразу:
```bash
npm run start_namespaces
```

### Компиляция и запуск вручную:
```bash
tsc src/samples/namespaces/index.ts
node src/samples/namespaces/index.js
```

### Отдельные файлы:
```bash
# Пример: только basic namespace
tsc src/samples/namespaces/01-basic-namespace/geometry.ts
node src/samples/namespaces/01-basic-namespace/geometry.js
```

---

## 📖 Порядок изучения

Рекомендуется изучать в следующем порядке:

1. **01-basic-namespace** - основы namespaces
2. **02-nested-namespaces** - вложенная структура
3. **03-namespace-merging** - слияние объявлений
4. **04-namespace-aliases** - упрощение кода
5. **05-namespace-with-interfaces** - типы и интерфейсы
6. **06-namespace-vs-module** - выбор подхода

---

## 🎓 Ключевые концепции по модулям

### Модуль 1: Basic Namespace
- ✅ `namespace Name { }`
- ✅ `export` для публичных членов
- ✅ Приватные функции без export

### Модуль 2: Nested Namespaces
- ✅ `namespace Outer.Inner { }`
- ✅ Иерархия и группировка
- ✅ Логическая организация

### Модуль 3: Namespace Merging
- ✅ Множественные объявления
- ✅ Автоматическое слияние
- ✅ Расширение API

### Модуль 4: Namespace Aliases
- ✅ `import Alias = Long.Path`
- ✅ Упрощение доступа
- ✅ Локальные имена

### Модуль 5: With Interfaces
- ✅ Интерфейсы и типы
- ✅ Енумы
- ✅ Generic классы

### Модуль 6: vs Module
- ✅ Сравнение подходов
- ✅ Когда использовать
- ✅ Best practices

---

## 💡 Советы по изучению

1. **Начните с `README.md`** - прочитайте теорию
2. **Изучите каждый модуль** - открывайте файлы по порядку
3. **Запускайте примеры** - видьте результаты в консоли
4. **Экспериментируйте** - изменяйте код и смотрите что происходит
5. **Используйте `NAMESPACES-CHEATSHEET.md`** - для быстрой справки
6. **Сравните с модулями** - понимайте разницу

---

## 🔗 Связь между модулями

```
01-basic-namespace          → Основа всех namespaces
        ↓
02-nested-namespaces        → Расширение: вложенность
        ↓
03-namespace-merging        → Расширение: слияние
        ↓
04-namespace-aliases        → Утилита: упрощение
        ↓
05-with-interfaces          → Практика: типы и интерфейсы
        ↓
06-vs-module                → Сравнение: выбор подхода
```

---

## 📊 Статистика

- **Всего примеров:** 6 разделов
- **Файлов кода:** 6 TypeScript файлов
- **Документации:** 3 markdown файла
- **Концепций:** Basic, Nested, Merging, Aliases, Types, Comparison
- **Паттернов:** Repository, API, Configuration

---

## 🎯 Что вы изучите

После прохождения всех примеров вы будете знать:

✅ Как объявлять и использовать namespaces  
✅ Как создавать вложенные namespaces  
✅ Как работает слияние namespaces  
✅ Как упрощать длинные пути с псевдонимами  
✅ Как организовать типы в namespaces  
✅ Разницу между namespace и module  
✅ Когда использовать namespace (и когда нет)  
✅ Best practices для namespaces  

---

## 🚦 Namespace vs Module - Выбор

### Используйте NAMESPACE для:
- ✅ Type definitions (.d.ts файлы)
- ✅ Внутренней организации библиотеки
- ✅ Легаси кода
- ✅ Простых скриптов

### Используйте MODULE для:
- ⭐ Новых проектов (рекомендуется!)
- ⭐ Больших приложений
- ⭐ Tree-shaking
- ⭐ npm пакетов

---

## 🐛 Common Issues

### Issue 1: "Cannot find namespace"
**Проблема:** Namespace не найден в другом файле  
**Решение:** Добавьте `/// <reference path="file.ts" />` или компилируйте вместе

### Issue 2: Слияние не работает
**Проблема:** Два namespace не объединяются  
**Решение:** Проверьте что имена написаны ТОЧНО одинаково (case-sensitive)

### Issue 3: Приватные члены доступны
**Проблема:** Функция без export всё равно доступна  
**Решение:** Убедитесь что используете скомпилированный .js файл

---

## 📚 Дополнительные ресурсы

- [TypeScript Handbook - Namespaces](https://www.typescriptlang.org/docs/handbook/namespaces.html)
- [Namespaces and Modules](https://www.typescriptlang.org/docs/handbook/namespaces-and-modules.html)
- [Declaration Files](https://www.typescriptlang.org/docs/handbook/declaration-files/introduction.html)

---

## 🎓 Заключение

**Namespaces** - это TypeScript-специфичный способ организации кода:
- ✅ Хороши для legacy кода и type definitions
- ✅ Простая вложенная структура
- ✅ Автоматическое слияние объявлений
- ❌ Не рекомендуются для новых проектов

**Modules (ES6)** - современный стандарт:
- ⭐ Рекомендуются для всех новых проектов
- ⭐ Tree-shaking поддержка
- ⭐ Лучшая изоляция кода

---

**Удачи в изучении namespaces TypeScript!** 🚀



