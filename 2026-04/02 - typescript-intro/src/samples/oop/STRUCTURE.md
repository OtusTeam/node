# 📁 Структура проекта ООП

Все примеры ООП теперь организованы в отдельные модули для лучшей читаемости и поддержки.

## 📂 Структура папок

```
src/samples/oop/
│
├── 01-basic-class/
│   ├── Person.ts          # Класс Person
│   └── demo.ts            # Демонстрация базового класса
│
├── 02-access-modifiers/
│   ├── BankAccount.ts     # Класс с модификаторами доступа
│   └── demo.ts            # Демонстрация модификаторов
│
├── 03-inheritance/
│   ├── Animal.ts          # Базовый класс Animal
│   ├── Dog.ts             # Класс Dog (наследник Animal)
│   ├── Cat.ts             # Класс Cat (наследник Animal)
│   └── demo.ts            # Демонстрация наследования
│
├── 04-polymorphism/
│   └── demo.ts            # Демонстрация полиморфизма
│
├── 05-abstract-classes/
│   ├── Shape.ts           # Абстрактный класс Shape
│   ├── Rectangle.ts       # Конкретная реализация
│   ├── Circle.ts          # Конкретная реализация
│   └── demo.ts            # Демонстрация абстрактных классов
│
├── 06-interfaces/
│   ├── IVehicle.ts        # Интерфейс IVehicle
│   ├── IElectric.ts       # Интерфейс IElectric
│   ├── Car.ts             # Класс, реализующий IVehicle
│   ├── ElectricCar.ts     # Класс, реализующий оба интерфейса
│   └── demo.ts            # Демонстрация интерфейсов
│
├── 07-getters-setters/
│   ├── Temperature.ts     # Класс с геттерами и сеттерами
│   └── demo.ts            # Демонстрация геттеров/сеттеров
│
├── 08-static-members/
│   ├── MathHelper.ts      # Класс со статическими членами
│   └── demo.ts            # Демонстрация статических членов
│
├── 09-readonly/
│   ├── Book.ts            # Класс с readonly свойствами
│   └── demo.ts            # Демонстрация readonly
│
├── 10-constructor-shorthand/
│   ├── User.ts            # Класс с сокращённой записью конструктора
│   └── demo.ts            # Демонстрация сокращённой записи
│
├── 11-composition/
│   ├── Engine.ts          # Компонент Engine
│   ├── Wheels.ts          # Компонент Wheels
│   ├── Automobile.ts      # Класс, использующий композицию
│   └── demo.ts            # Демонстрация композиции
│
├── 12-generics/
│   ├── Stack.ts           # Generic класс Stack<T>
│   └── demo.ts            # Демонстрация generic классов
│
├── index.ts               # 🚀 Главный файл - запускает все демо
├── README.md              # Документация
├── OOP-CHEATSHEET.md      # Шпаргалка по ООП
└── STRUCTURE.md           # Этот файл
```

## 🎯 Преимущества такой структуры

### ✅ Модульность
- Каждый концепт в своей папке
- Легко найти нужный пример
- Простота навигации

### ✅ Переиспользование
- Классы можно импортировать в другие проекты
- Чистое разделение ответственности
- Примеры реального мира

### ✅ Масштабируемость
- Легко добавлять новые примеры
- Не нужно редактировать большой файл
- Каждый файл отвечает за одну вещь

### ✅ Обучение
- Понятная структура для новичков
- Каждая папка = одна концепция
- Логическая последовательность 01, 02, 03...

## 📖 Как использовать

### Запуск всех примеров
```bash
npm run start_oop
```

### Импорт отдельных классов
```typescript
// Импорт конкретного класса
import { Person } from './01-basic-class/Person';
import { BankAccount } from './02-access-modifiers/BankAccount';
import { Stack } from './12-generics/Stack';

// Использование
const person = new Person("Иван", 30);
const stack = new Stack<number>();
```

### Изучение примеров
1. Начните с папки `01-basic-class/`
2. Изучите файл класса (например, `Person.ts`)
3. Посмотрите на `demo.ts` для примера использования
4. Переходите к следующей папке по порядку

## 🔍 Как работает index.ts

Главный файл `index.ts` импортирует все demo функции и запускает их по очереди:

```typescript
import { demoBasicClass } from './01-basic-class/demo';
import { demoAccessModifiers } from './02-access-modifiers/demo';
// ... и так далее

// Запуск всех демо
demoBasicClass();
demoAccessModifiers();
// ...
```

## 💡 Рекомендации

### Для обучения:
1. Читайте код в порядке нумерации папок (01 → 12)
2. Запускайте примеры после изучения каждого
3. Пробуйте модифицировать классы
4. Создавайте свои примеры в той же структуре

### Для разработки:
1. Используйте эту структуру как шаблон для своих проектов
2. Один класс = один файл (Single Responsibility)
3. Группируйте связанные классы в папки
4. Добавляйте demo.ts для документации использования

## 🎓 Последовательность изучения

```
01. Базовый класс              ← Начните здесь
    ↓
02. Модификаторы доступа       ← public, private, protected
    ↓
03. Наследование               ← extends, super
    ↓
04. Полиморфизм                ← override methods
    ↓
05. Абстрактные классы         ← abstract
    ↓
06. Интерфейсы                 ← implements
    ↓
07. Геттеры и Сеттеры          ← get/set
    ↓
08. Статические члены          ← static
    ↓
09. Readonly                   ← readonly
    ↓
10. Сокращённая запись         ← constructor shorthand
    ↓
11. Композиция                 ← composition over inheritance
    ↓
12. Дженерики                  ← generic classes
```

## 📚 Дополнительные файлы

- **README.md** - Полная документация всех концепций ООП
- **OOP-CHEATSHEET.md** - Быстрая справка и примеры кода
- **STRUCTURE.md** - Этот файл (описание структуры)

## 🚀 Быстрый старт

```bash
# 1. Запустить все примеры
npm run start_oop

# 2. Изучить структуру
# Откройте папку 01-basic-class/

# 3. Читайте код в таком порядке:
#    Person.ts → demo.ts → следующая папка

# 4. Экспериментируйте!
# Измените классы и посмотрите на результат
```

---

**Совет:** Эта структура следует принципам SOLID и best practices TypeScript/JavaScript проектов. Используйте её как reference для своих проектов!

